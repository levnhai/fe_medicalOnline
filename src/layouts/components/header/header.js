import classNames from 'classnames/bind';
import style from './header.module.scss';

const cx = classNames.bind(style);

function Header() {
  return <h2 >đây là headerr</h2>;
}

export default Header;
